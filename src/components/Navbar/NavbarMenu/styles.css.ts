import styled from "@emotion/styled";

const Styles = {
  Menu: styled.ul`
    margin: 0;
    padding-left: 0;
    list-style-type: none;
    display: flex;
    align-items: center;
    gap: 1.125rem;
  `,
};

export default Styles;
