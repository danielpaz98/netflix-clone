import styled from "@emotion/styled";

const Styles = {
  MemberLinks: styled.ul`
    display: flex;
    align-items: flex-start;
    flex-wrap: wrap;
    font-size: 0.8125rem;
    margin-bottom: 0.875rem;
    padding-left: 0;
  `,
};

export default Styles;
