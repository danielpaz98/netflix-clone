import styled from "@emotion/styled";

const Styles = {
  ProviderList: styled.ul`
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
  `,
};

export default Styles;
